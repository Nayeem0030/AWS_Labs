pipeline {
    agent {label 'Windows-Slave-USE2-AWS'}
    stages {
        stage('Submit Stack') {
            steps {
            sh "aws cloudformation create-stack --stack-name ecinstance10 --template-body file://ec2.yaml --tags Key=Name,Value=123 --region 'us-west-2'"
              }
             }
            }
            }
